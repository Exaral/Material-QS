# Android 16 Quick Settings replacement

A standalone accessibility-overlay QS replacement inspired by the supplied Android 16/LineageOS SystemUI APK. It uses an invisible 72dp top-edge trigger; swipe down to open the full-screen QS panel and swipe up to close. It deliberately does not attempt to replace privileged SystemUI itself.

Build: `gradle assembleDebug` or GitHub Actions.
